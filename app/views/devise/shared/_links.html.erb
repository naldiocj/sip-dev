<div class="text-center space-y-2 text-sm">
  <% if controller_name != 'sessions' %>
    <%= link_to "Já tem uma conta? Entrar", auth_session_path(resource_name), class: "text-blue-600 hover:text-blue-700 font-medium" %>
  <% end %>
  
  <% if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to "Criar nova conta", auth_registration_path(resource_name), class: "block text-blue-600 hover:text-blue-700 font-medium" %>
  <% end %>
  
  <% if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <%= link_to "Esqueceu a senha?", auth_password_path(resource_name), class: "block text-blue-600 hover:text-blue-700 font-medium" %>
  <% end %>
  
  <% if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= link_to "Não recebeu as instruções de confirmação?", auth_confirmation_path(resource_name), class: "block text-blue-600 hover:text-blue-700 font-medium" %>
  <% end %>
  
  <% if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= link_to "Não recebeu as instruções de desbloqueio?", auth_unlock_path(resource_name), class: "block text-blue-600 hover:text-blue-700 font-medium" %>
  <% end %>
  
  <% if devise_mapping.omniauthable? %>
    <% resource_class.omniauth_providers.each do |provider| %>
      <%= button_to "Entrar com #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false } %><br />
    <% end %>
  <% end %>
</div>
